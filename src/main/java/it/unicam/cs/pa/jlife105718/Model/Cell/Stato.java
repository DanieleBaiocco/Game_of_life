package it.unicam.cs.pa.jlife105718.Model.Cell;

/**
 * Enumerazione che rappresenta lo stato di una cellula. Questa può essere viva o morta in un dato momento
 */
public enum Stato {
    VIVO,MORTO
}
